"""

This file implements a simple hodograph for the jerk vector along the y(x) curve.

"""

from inputParams import parameters
from helpers import Uprime, veloc, create_plot
from math import pow, sqrt
from random import randint
import matplotlib.pyplot as plt

# unpack inputs from "inputParams.py"
Jt, Jf, Q, g, a0, v0, y0, xmax, tmax, h, dt = parameters.values()

def x_comp(U, y_i):
    pass


def y_comp(U, y_i):
    pass


